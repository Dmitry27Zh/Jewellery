'use strict';

(function () {
  const Breakpoints = {
    MOBILE: [320, 768],
    TABLET: [768, 1024],
    DESKTOP: [1024],
  };

  const SliderMode = {
    PREV: 'prev',
    NEXT: 'next',
    PAGE: 'page',
  };

  window.const = {
    Breakpoints,
    SliderMode,
  };
})();
